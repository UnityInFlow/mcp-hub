---
phase: 07-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - README.md
  - LICENSE
autonomous: true
requirements:
  - DIST-01
  - DIST-03
must_haves:
  truths:
    - "nix crate is a unix-only dependency in Cargo.toml so Windows cross-compilation succeeds"
    - "cargo publish --dry-run succeeds with no errors"
    - "README.md exists with project description for crates.io"
    - "LICENSE file exists with MIT license text"
  artifacts:
    - path: "Cargo.toml"
      provides: "nix under [target.'cfg(unix)'.dependencies], full crates.io metadata, readme field"
      contains: "target.'cfg(unix)'.dependencies"
    - path: "README.md"
      provides: "Project description, install instructions, usage overview for crates.io and GitHub"
      min_lines: 30
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
  key_links:
    - from: "Cargo.toml"
      to: "README.md"
      via: "readme field in [package]"
      pattern: 'readme\s*=\s*"README.md"'
---

<objective>
Prepare the mcp-hub crate for cross-platform distribution and crates.io publishing.

Purpose: The nix crate is currently an unconditional dependency which breaks Windows cross-compilation (DIST-01 blocker). The crate also needs README.md, LICENSE, and a readme field in Cargo.toml for crates.io publishing (DIST-03 enabler).

Output: Cargo.toml with unix-gated nix dependency, README.md, LICENSE, and passing `cargo publish --dry-run`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-distribution/07-CONTEXT.md
@.planning/phases/07-distribution/07-RESEARCH.md
@Cargo.toml

<interfaces>
<!-- Cargo.toml currently has nix as unconditional dependency at line 21: -->
<!-- nix = { version = "0.29", features = ["signal", "process"] } -->
<!-- All source usage of nix is already gated behind #[cfg(unix)] blocks -->
<!-- Source files using nix: daemon.rs, supervisor.rs, control.rs -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix nix dependency gating and add crates.io metadata</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (current dependency layout, verify line numbers)
    - src/daemon.rs (lines 1-20, confirm nix usage is cfg-gated)
    - src/supervisor.rs (lines 1-10, confirm nix usage is cfg-gated)
  </read_first>
  <action>
Modify Cargo.toml to move the `nix` dependency from `[dependencies]` to a platform-specific section per D-09 (crates.io metadata) and research pitfall #1 (nix breaks Windows):

1. Remove `nix = { version = "0.29", features = ["signal", "process"] }` from `[dependencies]`.
2. Add a new section AFTER `[dependencies]` and BEFORE `[dev-dependencies]`:
   ```toml
   [target.'cfg(unix)'.dependencies]
   nix = { version = "0.29", features = ["signal", "process"] }
   ```
3. Add `readme = "README.md"` to the `[package]` section (after the `categories` field).
4. Verify all other crates.io required fields are present: name, version, description, license, repository, keywords, categories. (Research confirms they are — verify and leave unchanged.)

Run `cargo check` to confirm compilation still succeeds after the move.
  </action>
  <verify>
    <automated>cd /Users/jirihermann/Documents/workspace-1-ideas/unity-in-flow-ai/07-mcp-hub && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "target.'cfg(unix)'.dependencies" Cargo.toml (nix is unix-gated)
    - ! grep -E "^\[dependencies\]" -A 30 Cargo.toml | grep -q "^nix" (nix is NOT in unconditional deps)
    - grep -q 'readme = "README.md"' Cargo.toml (readme field present)
    - cargo check succeeds with exit code 0
  </acceptance_criteria>
  <done>nix dependency is unix-only in Cargo.toml, readme field added, cargo check passes</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md and LICENSE files</name>
  <files>README.md, LICENSE</files>
  <read_first>
    - .planning/PROJECT.md (lines 1-30, project description and core value)
    - CLAUDE.md (lines 1-20, project overview)
  </read_first>
  <action>
Create two files required for crates.io publishing and GitHub presentation:

**README.md** — Create with the following sections:
- `# mcp-hub` — Title with one-line description: "PM2 for MCP servers — manage, monitor, and configure your MCP servers from a single binary."
- `## Features` — Bullet list of key capabilities: TOML config, process lifecycle (start/stop/restart), health monitoring with MCP ping, auto-restart with exponential backoff, unified log streaming, MCP introspection (tools/resources/prompts), web UI dashboard, config generation for Claude Code and Cursor, interactive init wizard, daemon mode.
- `## Installation` — Three methods:
  1. **Homebrew (macOS):** `brew install unityinflow/tap/mcp-hub`
  2. **Cargo:** `cargo install mcp-hub`
  3. **Pre-built binaries:** Download from GitHub Releases page link
- `## Quick Start` — Show: create `mcp-hub.toml` with one server entry, run `mcp-hub start`, check `mcp-hub status`, view `http://localhost:3456`.
- `## Configuration` — Show TOML config format with name, command, args, env, transport fields. Mention `[hub]` section with `web_port`.
- `## Commands` — Table of CLI commands: start, stop, restart, status, logs, gen-config, init.
- `## License` — "MIT" with link to LICENSE file.

**LICENSE** — Create MIT License file with:
- Year: 2026
- Copyright holder: "UnityInFlow Contributors"
- Full MIT license text (the standard OSI-approved text).

Do NOT use emojis in either file.
  </action>
  <verify>
    <automated>cd /Users/jirihermann/Documents/workspace-1-ideas/unity-in-flow-ai/07-mcp-hub && test -f README.md && test -f LICENSE && wc -l README.md LICENSE && cargo publish --dry-run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test -f README.md (README exists)
    - test -f LICENSE (LICENSE exists)
    - grep -q "MIT License" LICENSE (correct license type)
    - grep -q "## Installation" README.md (install section present)
    - grep -q "brew install" README.md (Homebrew install per D-08)
    - grep -q "cargo install mcp-hub" README.md (cargo install per D-09)
    - cargo publish --dry-run exits successfully
  </acceptance_criteria>
  <done>README.md has project description, installation methods (brew, cargo, binaries), usage overview. LICENSE has MIT text. cargo publish --dry-run succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo.toml -> crates.io | Package metadata uploaded to public registry |
| README.md -> GitHub/crates.io | Public-facing documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Cargo.toml metadata | accept | All fields are intentionally public (name, repo URL, license). No secrets in Cargo.toml. |
| T-07-02 | Tampering | crates.io publish | mitigate | cargo publish --dry-run validates package integrity before actual publish. CARGO_REGISTRY_TOKEN stored as GitHub secret (never in code). |
</threat_model>

<verification>
1. `cargo check` passes (nix gating correct)
2. `cargo publish --dry-run` passes (all crates.io metadata present, README included)
3. `grep "target.'cfg(unix)'.dependencies" Cargo.toml` matches (nix is platform-specific)
4. README.md and LICENSE both exist with correct content
</verification>

<success_criteria>
- nix dependency compiles only on Unix targets, enabling Windows cross-compilation
- cargo publish --dry-run succeeds, confirming crates.io readiness
- README.md documents installation via all three channels (brew, cargo, binaries)
- LICENSE contains MIT license text
</success_criteria>

<output>
After completion, create `.planning/phases/07-distribution/07-01-SUMMARY.md`
</output>
